import SignInPage from "./SignInPage";
import React from "react";
import { HashRouter } from "react-router-dom";

const App = () => {
  return (
    <HashRouter>
      <SignInPage />
    </HashRouter>
  );
};

export default App;
